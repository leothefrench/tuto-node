const num1 = 5
const num2 = 10

function addValues() {
    console.log(`the sum is : ${num1 + num2}`);
}

addValues()

// Une fois une fonction invoquée celle-ci fais ce qu'elle a à faire même si on est dans un autre fichier
